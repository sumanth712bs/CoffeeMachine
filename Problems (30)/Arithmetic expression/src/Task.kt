import java.util.*

fun main(args: Array<String>) {
    val scanner = Scanner(System.`in`)
    val firstVal = scanner.nextInt()
    val secondVal = scanner.nextInt()
    val result = firstVal + secondVal
    println("$firstVal plus $secondVal equals $result")
}